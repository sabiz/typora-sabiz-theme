{
  "name": "typora-sabiz-theme",
  "version": "3.0.0",
  "description": "Typora theme",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js -- dev",
    "deploy:dev": "run-s dev deploy",
    "deploy:build": "run-s build deploy",
    "deploy": "node scripts/install.js"
  },
  "devDependencies": {
    "autoprefixer": "^10.0.0",
    "cssnano": "^7.1.0",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1",
    "postcss-import": "^16.1.1"
  }
}
